<div class="pagina-seccion">Resultados del proceso de evaluación activo</div>

<ul class="lead my-3">
    {# Solo se debe mostrar en periodo entre fecha de resultado provisional y de resultado definitivo #}
    <li>
        <a id="verProvisional" href="">Resultado provisional del proceso de evaluación del desempeño</a>
    </li>
    {% if activos[0].configuracion.definitiva.date is defined and activos[0].configuracion.definitiva.date|date('c') < 'now'|date('c') %}
        {# Solo se debe mostrar cuando se cumpla la fecha de resultado definitivo #}
        <li>Resultado final del proceso de evaluación del desempeño</li>
    {% endif -%}
</ul>